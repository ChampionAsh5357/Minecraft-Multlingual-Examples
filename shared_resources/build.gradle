// Add plugins
plugins {
    id 'java-conventions'
}

// Set project information
classifier.id = ''
classifier.version = ''

sourceSets {
    forge {
        resources.srcDirs sourceSets.main.resources.srcDirs
    }
}
